---
description: QMK 个人固件项目 — 通用偏好与工作流
alwaysApply: true
---

# QMK 个人固件项目

## 用户背景

- **左手键盘用户**：常用快捷键优先放到键盘**右手区域**，方便单手操作
- 设计快捷键时，优先考虑右手侧的修饰键（如右 Ctrl）+ 方向键/邻近按键
- Mac/Windows 双模式键盘需用 layer 判断区分 Cmd 与 Ctrl
- **两块键盘**：物理上区分游戏 / 办公，但**固件逻辑默认通用**——快捷键与灯效模式代码各一份，改一处两边同步编译
- 只有用户**明确说明**某键「游戏专用」或「办公专用」时，才在对应 keymap 里分叉

## 项目结构

```
qmk_firmware/
  firmware/
    gaming/                  ← 游戏用键盘刷写目录
      keychron_q11/
    office/                  ← 办公用键盘刷写目录
      keychron_q11/
  keyboards/keychron/q11/
    q11_rgb/                   ← 灯效（共用）
    q11_shortcuts/             ← 快捷键（共用）
    ansi_encoder/keymaps/
      keymap.inc               ← 键位布局（共用）
      default/                 ← 办公：仅 config.h profile 不同
      gaming/
```

**不要**在 `C:\Tools\qmk\` 根目录或仓库外创建 `office_rgb/`、`gaming_rgb/` 等目录；所有源码放在 `qmk_firmware/keyboards/...` 内。编译产物只应出现在 `.build/` 或 `firmware/`。

## 我的键盘

| 用途 | 键盘 | 固件目录 | 编译脚本 |
|------|------|----------|----------|
| 游戏 | Keychron Q11 | `firmware/gaming/keychron_q11/` | `ansi_encoder/compile-gaming.ps1` |
| 办公 | Keychron Q11 | `firmware/office/keychron_q11/` | `ansi_encoder/compile.ps1` |

两块键盘源码路径相同：`keyboards/keychron/q11/ansi_encoder/`。

## 办公 vs 游戏的唯一差异

| | 办公 keymap `default` | 游戏 keymap `gaming` |
|--|----------------------|----------------------|
| 灯效循环 | `Q11_RGB_PROFILE_OFFICE`（5 种） | `Q11_RGB_PROFILE_GAMING`（2 种） |
| 快捷键 / 键位 | 共用 `q11_shortcuts/` + `keymap.inc` | 同上 |

## 灯光

详见 `q11_rgb/README.md`。纯色模式：←/→ 全局色相；↑/↓ 右半色相；PgUp/PgDn 左半饱和度；Ins/Del 右半饱和度。涟漪/波浪：←/→ 色相，PgUp/PgDn 饱和度。

## 工作流

1. 改快捷键 → `q11_shortcuts/q11_shortcuts.c`；改键位 → `keymaps/keymap.inc`；改灯效 → `q11_rgb/`
2. 分别运行 `compile.ps1` / `compile-gaming.ps1`
3. 用户自行刷写对应 `latest.bin`（不要自动刷固件）

## 编译环境（Windows）

```powershell
C:\QMK_MSYS\shell_connector.cmd -lc "cd /c/Tools/qmk/qmk_firmware && qmk compile -kb keychron/q11/ansi_encoder -km default"
```

具体映射见 `ansi_encoder/自定义说明.md` 与 Obsidian `Projects/QMK/`。
